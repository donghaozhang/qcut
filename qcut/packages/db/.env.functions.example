# Set on Supabase via:
#   supabase secrets set --env-file packages/db/.env.functions
#
# Do NOT commit the real .env.functions file. This .example shows the
# shape only.

SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
QCUT_IMAGE_TAG=qcut-cli:v0
RELAY_SIGNING_SECRET=
RELAY_HOST=relay.qcut.app
E2B_API_KEY=
