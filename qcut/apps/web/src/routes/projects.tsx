import { createFileRoute } from "@tanstack/react-router";

export const Route = createFileRoute("/projects")({
  // This file only contains critical route configuration
  // The component is defined in projects.lazy.tsx
});
