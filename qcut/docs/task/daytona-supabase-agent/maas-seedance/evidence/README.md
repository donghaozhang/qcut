# MaaS Seedance Evidence

Live smoke-test runs write timestamped artifacts under `runs/`.

Run directories can contain provider JSON, downloaded videos, and `ffprobe` output. Keep large generated media out of git; summarize successful runs in markdown instead.
